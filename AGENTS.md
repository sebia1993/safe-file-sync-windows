# Working agreements
Read CODEX_DEVELOPMENT_PLAN.md and docs/SAFETY_MODEL.md before implementation.
Source is read-only. Never delete, move, rename, write, truncate or create within source, including logs, manifests and databases.
Never use Robocopy /MOV, /MOVE, /MIR or /PURGE. No arbitrary user options.
Do not weaken safety checks to pass tests. Windows Actions is the automated validation authority.
Do not publish internal addresses, credentials, company data, paths or logs.

